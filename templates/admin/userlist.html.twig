{% extends 'admin/layout_admin.html.twig' %}

{% block content %}
    <br>
    <table>
        <thead>
            <tr>
                <th>
                    Nom
                </th>
                <th>
                    Prénom
                </th>
                <th>
                    Email
                </th>
                <th>
                    Éditer
                </th>
                <th>
                    Supprimer
                </th>
            </tr>
        </thead>
        <tbody>
            {% for user in users %}
                <tr>
                    <td>
                        {{ user.lastname }}
                    </td>
                    <td>
                        {{ user.firstname }}
                    </td>
                    <td>
                        {{ user.email }}
                    </td>
                    <td>
                        <a href="{{ path('admin_useredit', {id:user.id}) }}">&nbsp;&nbsp;&nbsp;&nbsp;&Rightarrow;</a> 
                    </td>
                    <td>
                        <a href="{{ path('admin_userdelete', {id:user.id}) }}">&nbsp;&nbsp;&nbsp;&nbsp;&cross;</a>
                    </td>
                    
                </tr>
                {% endfor %}
        </tbody>
    </table>
{% endblock %}
